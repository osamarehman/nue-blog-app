export default {
  // Specify the components directory
  components: '@components',

  // Ensure components are compiled
  build: {
    components: true
  },

  // Server configuration
  server: {
    port: 8081
  }
}
